# Use TestPyPI first (Agents SDK publishes here), with PyPI as a fallback
--index-url https://test.pypi.org/simple/
--extra-index-url https://pypi.org/simple

# --- Microsoft 365 Agents SDK (Python) ---
# Core protocol models & primitives used across the SDK
microsoft-agents-activity

# Builder layer (Agent, TurnContext, adapters, OAuth flow, state helpers)
microsoft-agents-builder

# **Base Storage namespace** (MemoryStorage, StoreItem) required by Builder/state
microsoft-agents-storage

# Hosting
microsoft-agents-hosting-core
microsoft-agents-hosting-aiohttp

# Auth bits (used by some hosting flows)
microsoft-agents-authorization
microsoft-agents-authentication-msal

# Optional storage providers you may want later (safe to keep, no harm if unused)
microsoft-agents-storage-blob
microsoft-agents-storage-cosmos

# --- Runtime deps you already had ---
pydantic>=2.11.0
openai>=1.40.0
python-dotenv>=1.0.0
requests>=2.31.0
beautifulsoup4>=4.12.3
aiohttp>=3.9.0
